{
  "name": "zeno",
  "version": "0.0.0",
  "description": "Zeno",
  "main": "build/index.js",
  "engines": {
    "node": ">= 16.13 <17"
  },
  "devDependencies": {
    "@types/express": "^4.17.14",
    "@types/jest": "~29.1",
    "@types/node": "~16",
    "@typescript-eslint/eslint-plugin": "~5.39",
    "@typescript-eslint/parser": "~5.39",
    "eslint": "~8.24",
    "eslint-config-prettier": "~8.5",
    "eslint-plugin-jest": "~27.1",
    "jest": "~29.1",
    "prettier": "~2.7",
    "rimraf": "~3.0",
    "ts-jest": "~29.0",
    "ts-node": "^10.9.1",
    "tsutils": "~3.21",
    "typescript": "~4.8"
  },
  "scripts": {
    "start": "node build/src/main.js",
    "clean": "rimraf coverage build tmp",
    "prebuild": "npm run lint",
    "build": "tsc -p tsconfig.json",
    "build:watch": "tsc -w -p tsconfig.json",
    "build:release": "npm run clean && tsc -p tsconfig.release.json",
    "type-check": "tsc --pretty --noEmit --project tsconfig-base.json",
    "type-check-ci": "tsc --noEmit --project pipeline/tsconfig-base.json",
    "lint": "eslint . --ext .ts --ext .mts",
    "test": "jest --coverage",
    "prettier": "prettier --config .prettierrc --write .",
    "test:watch": "jest --watch"
  },
  "license": "MIT",
  "dependencies": {
    "express": "^4.18.2",
    "ipaddr.js": "^2.0.1",
    "js-yaml": "^4.1.0",
    "redis": "^4.3.1",
    "tslib": "~2.4"
  },
  "volta": {
    "node": "16.17.0"
  }
}
